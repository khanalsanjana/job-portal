<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employer Hub</title>
    <style>
        /* Reset and base styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body, html {
            font-family: 'Poppins', sans-serif;
            /* background: url('https://source.unsplash.com/1600x900/?business,technology') no-repeat center center/cover; */
            color: #fff;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background: url('https://images.unsplash.com/photo-1522071820081-009f0129c71c?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80') no-repeat center center fixed;
            background-size: cover;
            
        }
        
        /* Navbar */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: #fff;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 100;
        }
        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: black;
        }
        .nav-links {
            display: flex;
            gap: 15px;
        }
        .nav-links a {
            color: #fff;
            text-decoration: none;
            font-size: 1rem;
            padding: 0.5rem 1rem;
            background: #ff5733;
            border-radius: 5px;
            transition: 0.3s;
        }
        .nav-links a:hover {
            background: #c70039;
        }

        /* Header */
        header {
            
            text-align: center;
            margin-top: 100px;
            padding: 2rem;
            font-size: 2.5rem;
            font-weight: bold;
            /* text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.7); */
        }
        .whole-body{
            color: black;
        background: rgba(255, 255, 255, 0.8);
        backdrop-filter: blur(5px);
        }
        /* Section */
        section {
            
            margin-top: 30px;
            padding: 20px;
            text-align: center;
        }
        .company-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }
        .company-card {
            background: rgba(0, 0, 0, 0.85);
            padding: 20px;
            border-radius: 8px;
            width: 300px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s;
            color: white;
        }
        .company-card:hover {
            transform: scale(1.05);
        }
        .company-card h3 {
            color: white;
            font-size: 1.4rem;
            margin-bottom: 10px;
        }
        .company-card p {
            font-size: 1rem;
            margin-bottom: 10px;
        }
        .manage-btn {
            display: inline-block;
            padding: 8px 15px;
            background: #ff5733;
            color: white;
            border-radius: 5px;
            text-decoration: none;
            transition: 0.3s;
        }
        .manage-btn:hover {
            background: rgb(164, 135, 97);
        }

        /* Footer */
        footer {
            margin-top: auto;
            text-align: center;
            padding: 15px;
            background: rgba(0, 0, 0, 0.8);
            color: #ddd;
            /* margin: 70px; */
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav>
        <div class="logo">Companies Dashboard</div>
        <div class="nav-links">
            <a href="/company/register">Add Company</a>
            <a href="/logout">Logout</a>
        </div>
    </nav>

    <!-- Header -->
    <section class="whole-body">
    <header>
        Empowering Companies, Connecting Talent
    </header>

    <!-- Registered Companies Section -->
    <section>
        <h2>Registered Companies</h2>
        <div class="company-container">
            <% if (companies && companies.length > 0) { %>
                <% companies.forEach(company => { %>
                    <div class="company-card">
                        <h3><%= company.companyName %></h3>
                        <p><strong>Address:</strong> <%= company.address %></p>
                        <p><strong>Contact:</strong> <%= company.contactInfo %></p>
                        <a href="/company/<%= company.companyId %>/jobs" class="manage-btn">Manage Jobs</a>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No companies registered yet.</p>
            <% } %>
        </div>
    </section>

</section>
    <!-- Footer -->
    <footer>
        &copy; 2025 Companies . All rights reserved.
    </footer>

</body>
</html>
